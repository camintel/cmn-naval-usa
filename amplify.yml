version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static homepage artifact; no build step required."
  artifacts:
    baseDirectory: .
    files:
      - assets/**/*
      - contact/**/*
      - fleet/**/*
      - maritime-logistics/**/*
      - index.html
